

function greeter(person: string): string {
  return "Hello, " + person;
}

const config = {
  name: 'xx',
  age: 12,
  sex: 'female'
}

const mergeConfig = {...config, name: 'dd'};


class Animal {
  constructor(props: object) {

  }
}

const fetchRequest = (param: string): Promise<string> => {
  return new Promise((resolve: any, reject: any) => {})
}

