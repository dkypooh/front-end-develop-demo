function greeter(person) {
    return "Hello, " + person;
}
const config = {
    name: 'xx',
    age: 12,
    sex: 'female'
};
const mergeConfig = Object.assign({}, config, { name: 'dd' });
class Animal {
    constructor(props) {
    }
}
new Promise((resolve, reject) => { });
